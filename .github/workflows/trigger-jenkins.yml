name: Trigger Jenkins

on:
  push:
    branches: master

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins build
        run: |
          curl -X POST http://ip10-0-39-6-d33tefhntdlhbpdsdcu0-8080.direct.lab-boris.fr/job/iac-pipeline/build --user "ok-blud:Wvaax29267" 
